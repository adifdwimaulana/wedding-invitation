# Copy to .env for local development, and set the same variable in the Netlify
# UI under Site configuration > Environment variables for production.
#
# Gates /admin. Use something long and random — this is the only thing standing
# between the public internet and every guest's RSVP:
#   openssl rand -base64 24
ADMIN_PASSWORD=
